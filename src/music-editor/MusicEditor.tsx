import { Component } from "solid-js";

const MusicEditor: Component<{}> = (props) => {
  return (
    <div
      style={{
        "flex-grow": "1",
      }}
    >

    </div>
  );
};

export default MusicEditor;
